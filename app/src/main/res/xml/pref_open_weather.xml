<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:capitalize="none"
        android:defaultValue="@string/pref_default_location_id"
        android:inputType="numberDecimal"
        android:key="open_weather_location_id"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_location_id" />

    <EditTextPreference
        android:capitalize="none"
        android:defaultValue="@string/pref_default_api_key"
        android:inputType="textNoSuggestions"
        android:key="open_weather_api_key"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_api_key" />

</PreferenceScreen>
